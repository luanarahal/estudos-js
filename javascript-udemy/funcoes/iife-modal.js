//var isValid = true //usando var, ambas as isValid são declaradas globalmente, então a modal sobrescreverá a menu, porque está por último no html
function init() {
    console.log("init modal")
}
init()