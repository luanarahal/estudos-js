console.log('Javascript funcionando!');

let nome = "Luana"
console.log(10 + 20)

// ignora uma única linha

/* comentário
de 
várias
linhas
*/

//JS é casesensitive - diferencia letras minúsculas e maiúsculas
//console.log(Nome); - não iria registrar a variável "nome" lá em cima, pois está escrito "Nome"

//palavras reservadas js - não pode usar como nome de variável
/* abstract	arguments	await	boolean
break	byte	case	catch
char	class	const	continue
debugger	default	delete	do
double	else	enum	eval
export	extends	false	final
finally	float	for	function
goto	if	implements	import
in	instanceof	int	interface
let	 long	native	new
null	package	private	protected
public	return	short	static
super	 switch	synchronized	this
throw	throws	transient	true
try	typeof	var	void
volatile	while	with	yield */

//funções e variáveis começam com letra minúsculas 'ex: function getName()', pois quando começam com letras maíusculas 'ex: GetName()' geralmente significam funções construtoras ou clássicas, não é obrigatório, não impede, mas é usual. 

//quando a função ou variável tem mais de um nome, geralmente é escrito desta forma 'ex: getName()' com a primeira letra das próximas palavras em maiúsculo

//não pode iniciar o nome da uma função ou variável com número nem caractere especial (exceto _ e $, 'ex: let _n1 = 0' ou 'ex: let $n1 = 0'), mas a partir do primeiro caractere, pode utilizar. 'ex: let 1g = 0' não é permitido, mas 'ex: let g1 = 0' é permitido